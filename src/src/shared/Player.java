package shared;

import javafx.scene.image.Image;

/**
 * Created by phani on 3/25/2017.
 */
public class Player extends Person {
    public Player(String name, Image image, int x, int y, int radius) {
        super(name, image, x, y, radius);
    }
}
